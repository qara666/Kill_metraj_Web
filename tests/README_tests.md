Test plan for TTL adaptive window
- ttl_config_test.js: validates TTL config values
- pseudo_end_to_end_ttl.js: simple TTL logic validation in isolation
- No DB access in these tests; these are unit-ish tests to validate logic
